# Stock 股票模块

## 概述

Stock 模块用于管理股票的基本信息和实时行情数据。每个股票代码唯一，存储股票的基础信息（代码、名称、市场）和实时行情数据（价格、成交量、市值等）。行情数据通过 Quotes 模块自动同步更新。

## 设计理念

### 数据表职责划分

```
📋 stocks 表  - 存储股票基本信息 + 实时行情关键指标
📊 quotes 表  - 存储每只股票的最新行情快照（完整字段）
📈 trends 表  - 存储分时数据（历史，按分钟）
📉 kline 表   - 存储K线数据（历史，日/周/月）
```

### 核心特性

- ✅ **唯一股票记录**：每个股票代码唯一，通过 code 字段确保
- ✅ **自动同步**：从东方财富 API 获取股票基本信息
- ✅ **Upsert 策略**：存在则更新，不存在则创建
- ✅ **实时行情集成**：包含从 quotes 表同步的实时行情数据
- ✅ **市场分类**：支持按市场代码和市场类型筛选
- ✅ **只读数据**：股票信息通过 API 同步，不允许手动修改

## 数据模型

### Stock 实体字段

| 字段名 | 类型 | 说明 | 精度 | 索引 |
|--------|------|------|------|------|
| **id** | number | 主键ID | 自动生成 | - |
| **code** | string | 股票代码（如 600519） | 20字符 | 唯一索引 |
| **name** | string | 股票名称 | 100字符 | - |
| **market** | number | 市场代码（1-上交所、0-深交所） | int | 普通索引 |
| **marketType** | string | 市场类型（SH-上海、SZ-深圳） | 20字符 | - |
| **price** | decimal | 最新价（元） | 12,4 | - |
| **pct** | decimal | 涨跌幅（%） | 10,4 | - |
| **change** | decimal | 涨跌额（元） | 12,4 | - |
| **volume** | bigint | 成交量（股） | - | - |
| **amount** | decimal | 成交额（元） | 20,4 | - |
| **totalMarketCap** | decimal | 总市值（元） | 20,4 | - |
| **floatMarketCap** | decimal | 流通市值（元） | 20,4 | - |
| **turnover** | decimal | 换手率（%） | 10,4 | - |
| **createdAt** | Date | 创建时间 | 自动生成 | - |
| **updatedAt** | Date | 更新时间 | 自动管理 | - |

### 字段分类

#### 基本信息字段
- `code` - 股票代码（唯一标识）
- `name` - 股票名称
- `market` - 市场代码
- `marketType` - 市场类型

#### 实时行情字段（从 quotes 同步）
- `price` - 最新价
- `pct` - 涨跌幅
- `change` - 涨跌额
- `volume` - 成交量
- `amount` - 成交额
- `turnover` - 换手率
- `totalMarketCap` - 总市值
- `floatMarketCap` - 流通市值

### 数据库索引

```typescript
@Index(['code'], { unique: true })  // 股票代码唯一索引
@Index(['market'])                   // 市场代码索引，用于按市场筛选
```

## API 接口

### 数据同步

#### 1. 同步股票信息

**接口**: `POST /stocks/sync`

**说明**: 通过东方财富 API 获取股票基本信息，如果股票不存在则新增，存在则更新

**请求体**:
```json
{
  "code": "600519",
  "market": 1
}
```

**响应**:
```json
{
  "code": 200,
  "message": "股票信息同步成功",
  "data": {
    "stock": {
      "id": 1,
      "code": "600519",
      "name": "贵州茅台",
      "market": 1,
      "marketType": "SH",
      "price": 1850.00,
      "pct": 2.35,
      "change": 43.00,
      "volume": 1234567,
      "amount": 228395450.00,
      "turnover": 0.98,
      "totalMarketCap": 2324500000000.00,
      "floatMarketCap": 2324500000000.00,
      "createdAt": "2024-01-21T10:30:00.000Z",
      "updatedAt": "2024-01-21T15:00:00.000Z"
    },
    "isNew": false
  }
}
```

**执行流程**:
1. 根据 code 查询股票是否已存在
2. 如果不存在，从 API 获取股票信息并创建新记录
3. 如果存在，返回现有记录（基本信息通常不变）
4. 返回股票信息和 `isNew` 标识（true-新增，false-已存在）

### 数据查询

#### 2. 获取股票列表

**接口**: `POST /stocks/list`

**说明**: 获取股票列表，支持按市场代码或市场类型筛选

**请求体**:
```json
{
  "market": 1,           // 可选，市场代码（1-上交所，0-深交所）
  "marketType": "SH"     // 可选，市场类型（SH-上海，SZ-深圳）
}
```

**响应**:
```json
{
  "code": 200,
  "message": "股票列表获取成功",
  "data": [
    {
      "id": 1,
      "code": "600519",
      "name": "贵州茅台",
      "market": 1,
      "marketType": "SH",
      "price": 1850.00,
      "pct": 2.35,
      "volume": 1234567,
      "amount": 228395450.00,
      "totalMarketCap": 2324500000000.00
    }
  ]
}
```

**筛选规则**:
- 如果提供 `market`，返回指定市场的股票
- 如果提供 `marketType`，返回指定市场类型的股票
- 如果都不提供，返回所有股票
- 优先使用 `market` 参数

#### 3. 根据股票代码获取股票

**接口**: `POST /stocks/get-by-code`

**说明**: 根据股票代码获取单个股票的详细信息

**请求体**:
```json
{
  "code": "600519"
}
```

**响应**:
```json
{
  "code": 200,
  "message": "股票信息获取成功",
  "data": {
    "id": 1,
    "code": "600519",
    "name": "贵州茅台",
    "market": 1,
    "marketType": "SH",
    "price": 1850.00,
    "pct": 2.35,
    "change": 43.00,
    "volume": 1234567,
    "amount": 228395450.00,
    "turnover": 0.98,
    "totalMarketCap": 2324500000000.00,
    "floatMarketCap": 2324500000000.00,
    "createdAt": "2024-01-21T10:30:00.000Z",
    "updatedAt": "2024-01-21T15:00:00.000Z"
  }
}
```

**错误响应**（股票未找到）:
```json
{
  "code": 404,
  "message": "股票未找到",
  "data": null
}
```

### 数据管理

#### 4. 删除股票

**接口**: `POST /stocks/delete`

**说明**: 根据 ID 删除指定的股票记录

**请求体**:
```json
{
  "id": 1
}
```

**响应**:
```json
{
  "code": 200,
  "message": "股票删除成功",
  "data": {
    "success": true
  }
}
```

## 数据同步机制

### 与 Quotes 模块的协同

当通过 Quotes 模块同步行情数据时，会自动更新 Stock 表的实时行情字段：

```typescript
// quotes.service.ts 中的同步逻辑
await this.stockService.updateStockByCode(stock.code, {
  price: quote.price,                // 最新价
  pct: quote.pct,                    // 涨跌幅
  change: quote.change,              // 涨跌额
  volume: quote.volume,              // 成交量
  amount: quote.amount,              // 成交额
  turnover: quote.turnover,          // 换手率
  totalMarketCap: quote.totalMarketCap,    // 总市值
  floatMarketCap: quote.floatMarketCap,    // 流通市值
});
```

### 数据流向

```
东方财富 API
    ↓
Quotes 同步 → Quote 表（完整行情快照）
    ↓
Stock 表（实时行情关键指标）
```

### 设计优势

✅ **查询效率高**
- 查询股票列表时，直接从 Stock 表获取所有关键指标
- 无需 join Quote 表，减少查询复杂度

✅ **数据完整性**
- Stock 表包含基本信息 + 实时行情
- Quote 表包含完整的行情快照（含开高低收、估值等）

✅ **职责清晰**
- Stock 表：股票档案 + 实时行情概览
- Quote 表：详细行情快照

## 使用示例

### 1. 同步单只股票信息

```typescript
import { StockService } from './stock/stock.service';

@Injectable()
export class DataSyncService {
  constructor(private readonly stockService: StockService) {}

  async syncStock(code: string, market: number) {
    const result = await this.stockService.syncStockFromAPI(code, market);
    
    if (result.isNew) {
      console.log(`新增股票: ${result.stock.name}(${result.stock.code})`);
    } else {
      console.log(`股票已存在: ${result.stock.name}(${result.stock.code})`);
    }
    
    return result.stock;
  }
}
```

### 2. 查询所有股票

```typescript
async getAllStocks() {
  return await this.stockService.findAll();
}
```

### 3. 按市场查询股票

```typescript
// 查询上交所股票
async getShStocks() {
  return await this.stockService.findByMarket(1);
}

// 查询深交所股票
async getSzStocks() {
  return await this.stockService.findByMarket(0);
}

// 按市场类型查询
async getStocksByMarketType(marketType: string) {
  return await this.stockService.findByMarketType(marketType);
}
```

### 4. 查询单个股票

```typescript
async getStockInfo(code: string) {
  const stock = await this.stockService.findByCode(code);
  
  if (!stock) {
    throw new Error('股票未找到');
  }
  
  return stock;
}
```

### 5. 更新股票行情（内部使用）

```typescript
// 这个方法主要由 Quotes 模块调用
async updateStockQuote(code: string, quoteData: Partial<Stock>) {
  return await this.stockService.updateStockByCode(code, quoteData);
}
```

## 数据来源

### 股票基本信息

Stock 模块的基本信息（code, name, market, marketType）来自东方财富 API：

```typescript
import { eastmoney } from 'eastmoney-data-sdk';

// 获取股票基本信息
const stockInfo = await eastmoney.stock(code);
```

### 实时行情数据

实时行情数据（price, pct, change, volume, amount, turnover, totalMarketCap, floatMarketCap）由 Quotes 模块同步：

```typescript
// Quotes 模块调用
const quote = await eastmoney.quote(secid);
await stockService.updateStockByCode(code, quoteData);
```

## 市场代码说明

### market（市场代码）

| 代码 | 市场 | 说明 |
|------|------|------|
| 1 | 上交所 | 上海证券交易所 |
| 0 | 深交所 | 深圳证券交易所 |

### marketType（市场类型）

| 类型 | 市场 | 股票代码特征 |
|------|------|-------------|
| SH | 上海 | 6 开头（如 600519） |
| SZ | 深圳 | 0 或 3 开头（如 000001、300001） |

## 注意事项

1. **股票代码唯一性**: code 字段有唯一索引，同一个股票代码只能存在一条记录
2. **数据只读**: 股票信息通过 API 同步，不允许手动创建或修改
3. **行情数据同步**: 实时行情字段由 Quotes 模块自动更新
4. **基本信息稳定**: 股票的基本信息（code, name, market）通常不会变化
5. **Upsert 策略**: 同步时自动判断是新增还是更新
6. **删除谨慎**: 删除股票会影响相关的行情、分时、K线数据

## 性能优化

1. **唯一索引**: 在 code 字段建立唯一索引，快速查找
2. **市场索引**: 在 market 字段建立索引，支持快速按市场筛选
3. **单表查询**: Stock 表包含实时行情，避免频繁 join
4. **批量操作**: 支持批量查询和更新

## 错误处理

所有接口都使用统一的 ApiResponse 格式：

**成功响应**:
```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

**错误响应**:
```json
{
  "code": 400/404/500,
  "message": "错误描述",
  "data": null
}
```

常见错误码：
- `200` - 操作成功
- `400` - 请求参数错误
- `404` - 资源不存在（如股票未找到）
- `500` - 服务器内部错误

## 与其他模块的关系

```
┌─────────────┐
│   Stocks    │ ← 存储股票基本信息 + 实时行情关键指标
└─────────────┘
       ↑
       │ 同步更新行情
       │
┌─────────────┐
│   Quotes    │ ← 存储最新行情快照（完整字段）
└─────────────┘
       ↑
       │ API 获取
       │
┌─────────────┐
│  Eastmoney  │ ← 东方财富数据源
│     SDK     │
└─────────────┘
```

历史数据模块：
- **Trends 模块**：分时数据（按分钟）
- **Kline 模块**：K线数据（日/周/月）

## 最佳实践

1. **先同步股票，再同步行情**: 确保 Stock 记录存在后再同步 Quote 数据
2. **使用代码查询**: 优先使用 `findByCode()` 而不是直接查询 ID
3. **市场筛选**: 需要按市场分类时使用 `findByMarket()` 或 `findByMarketType()`
4. **行情更新**: 通过 Quotes 模块同步，不要直接修改 Stock 的行情字段
5. **定期清理**: 根据业务需要清理不再关注的股票数据

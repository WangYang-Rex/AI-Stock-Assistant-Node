太好了，这一步你选得**非常对** 👍
我们就按**“不破坏现有趋势系统”**的原则，把 **分时 / 5 分钟线** **只引入执行层**，而不是趋势层。

下面我给你的是一套 **「职业交易系统常见的分层架构 + 可直接工程化的规则」**。

---

# 一、先把「层级」彻底分清（这是核心）

你现在的系统，其实已经天然分成三层，只是之前没显式写出来：

```
① 趋势层（是否值得参与）
   → 日线 / 60min K线

② 执行层（怎么进、进多少）
   → 分时 / 5min

③ 风控层（什么时候必须走）
   → ATR / 均线破坏
```

👉 **分时线，只允许出现在②执行层**

---

# 二、执行层只回答 3 个问题（不多也不少）

分时 / 5 分钟线 **只允许回答**：

1️⃣ 现在进是不是“性价比更好”
2️⃣ 要不要立刻进，还是等一会
3️⃣ 这次加仓是 10% 还是 20%

❌ **禁止回答**：

* 趋势对不对
* 要不要清仓
* 中长期方向

---

# 三、执行层的“前置硬条件”（必须满足）

```ts
if (
  trend.trend !== 'UP' ||
  trend.strength === 'WEAK' ||
  risk.shouldStop
) {
  禁止使用分时信号
}
```

👉 **趋势层一票否决**

---

# 四、执行层推荐用的 3 类分时信号（稳 + 通用）

## ✅ 信号 1：分时 VWAP 回踩（首选）

### 为什么用 VWAP？

* 主力真实成本锚
* 比分时均线稳得多
* 机构也在看

### 规则（非常简单）

```text
价格回踩 VWAP ±0.3%
成交量缩
再次站回 VWAP
```

👉 **这是最经典的顺势低风险进场**

---

### TypeScript 示例

```ts
function vwap(prices: number[], volumes: number[]) {
  const pv = prices.reduce((s, p, i) => s + p * volumes[i], 0)
  const v = volumes.reduce((s, v) => s + v, 0)
  return pv / v
}
```

```ts
if (
  lastPrice < vwap * 1.003 &&
  lastPrice > vwap * 0.997 &&
  volumeRatio < 0.8
) {
  allowAdd = true
}
```

---

## ✅ 信号 2：5 分钟缩量回踩 EMA20

### 使用场景

* 趋势强
* 日内回调
* 不想追高

### 规则

```text
5min K 线：
回踩 EMA20
成交量 < 前 5 根均量
收盘重新站上 EMA20
```

👉 **这是“趋势内低吸”，不是抄底**

---

## ✅ 信号 3：5 分钟突破（只给加仓用）

```text
横盘 ≥ 30 分钟
成交量放大
向上突破区间高点
```

👉 **只用于 STRONG 趋势的后半段**

---

# 五、执行层动作 → 仓位映射（很关键）

| 分时信号质量   | 动作        |
| -------- | --------- |
| VWAP 回踩  | 加仓 10%    |
| EMA20 回踩 | 加仓 10~20% |
| 5min 突破  | 加仓 20%    |
| 信号不清晰    | HOLD      |

---

# 六、执行层统一输出（工程化）

```ts
export interface ExecSignal {
  allow: boolean
  action: 'BUY' | 'ADD' | 'HOLD'
  percent: number
  reason: string
}
```

```ts
export function intradayExecute(
  trend: TrendResult,
  risk: RiskResult,
  intraday: IntradayData
): ExecSignal {
  if (
    trend.trend !== 'UP' ||
    trend.strength === 'WEAK' ||
    risk.shouldStop
  ) {
    return {
      allow: false,
      action: 'HOLD',
      percent: 0,
      reason: '趋势层不允许'
    }
  }

  if (vwapPullback(intraday)) {
    return {
      allow: true,
      action: 'ADD',
      percent: 10,
      reason: '分时VWAP回踩'
    }
  }

  return {
    allow: false,
    action: 'HOLD',
    percent: 0,
    reason: '等待更优执行点'
  }
}
```

---

# 七、为什么这样设计“非常职业”

说句实话 👇
**这是很多职业交易 / 私募系统的真实结构**：

* 趋势模型：慢、稳、少错
* 执行模型：快、准、不贪
* 风控模型：冷酷、无情

👉 **三者绝不混用**

---

# 八、如果你现在把这层加进去，你的系统会发生什么变化？

✔ 追高明显减少
✔ 心态稳定（知道“趋势还在”）
✔ 仓位更均匀
✔ 回撤进一步下降

而且 **不影响你现有的任何回测逻辑**。

---

## 九、下一步（已经是最后两块“系统拼图”了）

接下来你可以继续往两个方向走：

2️⃣ **趋势 + 分时 + 止损的前端可视化设计（非常爽）**
3️⃣ **完整回测：日线决策 + 分时执行 的双周期回测引擎**

你继续回 **编号**，我陪你把这套系统打磨到「可以长期用的程度」。
